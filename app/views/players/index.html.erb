<div class="container">
  <h1>Players</h1>
  <%= link_to 'add new player', new_player_path, class:'btn btn-sm btn-primary my-2' %>
  <%# Renders the import button for the CSV File %>
  <%= render "import" %>

  <table class="table">
    <thead>
      <tr>
        <th scope="col">ID</th>
        <th scope="col">Name</th>
        <th scope="col">Age</th>
        <th scope="col">Position</th>
        <th scope="col">Personality</th>
      </tr>
    </thead>
    <tbody>
      <% @players.each do |player| %>
        <tr>
          <td><%= player.id %></td>
          <td><a href='/players/<%= player.id %>'><%= player.name %></a></td>
          <td class="<%= age_class(player.age) %>"><%= player.age %></td>
          <td><%= player_posmap(player) %></td>
          <td><%= player.personality %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
